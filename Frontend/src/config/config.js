let config = {
    maskDetectInterval: 200,
    maskDetectIntervalLarge: 500,
    maskDetectIntervalNormal: 200,
    fileUpload:{
        maxSizeLimit: 20,   //MB
        allowFormat: ["jpg", "jpeg", "png", "bmp"]
    }
};

export default config
